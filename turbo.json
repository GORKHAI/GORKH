{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["**/.env.*local"],
  "globalEnv": [
    "NODE_ENV",
    "PORT",
    "NEXT_PUBLIC_API_BASE",
    "API_WS_URL",
    "VITE_API_HTTP_BASE",
    "VITE_API_WS_URL",
    "VITE_DESKTOP_ALLOW_INSECURE_LOCALHOST",
    "VITE_DESKTOP_UPDATER_ENABLED",
    "VITE_DESKTOP_UPDATER_PUBLIC_KEY"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**", "src-tauri/target/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    }
  }
}
