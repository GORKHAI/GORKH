# GORKH v0.0.51 Stable Release Notes

## Release Metadata Sync

This release carries forward the premium workstation unification work from v0.0.50 and fixes the release metadata mismatch that blocked the desktop release workflow.

Synced release sources:

- `VERSION`
- `apps/api/package.json`
- `apps/desktop/package.json`
- `apps/desktop/src-tauri/Cargo.toml`
- `apps/desktop/src-tauri/tauri.conf.json`

## Safety Boundary

No signing, broadcasting, swaps, lending execution, LP execution, staking, program deployment, arbitrary RPC passthrough, Jito, Squads execution, hardware signing, Drift integration, or autonomous execution was added.

## Release Note

`v0.0.50` was already pushed and is not reused. `v0.0.51` is the next stable tag with checked-in version metadata aligned to the tag.
